export class ListadoPokemon{
    constructor(urlAnterior, urlSiguiente, listadoPokemones =[]){
        this.urlAnterior = urlAnterior,
        this.urlSiguiente = urlSiguiente,
        this.listadoPokemones = listadoPokemones
    }
}